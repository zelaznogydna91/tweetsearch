import styled from 'styled-components/macro'
import u from 'utils'

export default {
  HashtagList: styled.div`
  display: flex;
  column-gap: 12px;
  background-color: ${u.getColor()};
  flex-wrap: wrap;
`,
}
